{{ 'section-home.css' | asset_url | stylesheet_tag }}
{{ 'section-home-visit.css' | asset_url | stylesheet_tag }}
{% render 'section-appearance', section: section %}
<!-- ──────────── STORE VISIT ──────────── -->
<section class="homevisit" style="background-color: #FFFFFF; --title-scale: {{ section.settings.title_scale | default: 100 | divided_by: 100.0 }};">
  <div class="homevisit__wrap">
    <div>
      <div class="homevisit__eyebrow">{{ section.settings.eyebrow }}</div>
      <h2>{{ section.settings.title }}</h2>
      <p>{{ section.settings.text }}</p>
      <div class="homevisit__hours">
        {%- for block in section.blocks -%}
          <div class="homevisit__hr{% if block.settings.closed %} closed{% endif %}{% if block.settings.highlight %} today{% endif %}" {{ block.shopify_attributes }}><span class="d">{{ block.settings.day }}</span><span class="t">{{ block.settings.time }}</span></div>
        {%- endfor -%}
      </div>
      {% render 'button', label: section.settings.cta_label, href: section.settings.cta_link | default: pages.kontaktai.url | default: routes.root_url, style: 'primary', arrow: true, class: 'homevisit__cta' %}
    </div>
    <div class="homevisit__map">
      <svg viewBox="0 0 400 275" preserveAspectRatio="xMidYMid slice" aria-hidden="true">
        <rect width="400" height="275" fill="#E7E4DD"></rect>
        <g stroke="#D5D0C5" stroke-width="10" fill="none">
          <path d="M-20 100 L420 70"></path>
          <path d="M-20 190 L420 165"></path>
          <path d="M80 -20 L120 295"></path>
          <path d="M250 -20 L290 295"></path>
        </g>
        <g stroke="#CFC9BD" stroke-width="4" fill="none">
          <path d="M-20 45 L420 22"></path>
          <path d="M-20 145 L420 120"></path>
          <path d="M-20 240 L420 225"></path>
          <path d="M170 -20 L200 295"></path>
          <path d="M340 -20 L370 295"></path>
        </g>
        <g fill="#DAD5CA">
          <rect x="130" y="80" width="100" height="75" rx="3"></rect>
          <rect x="300" y="175" width="70" height="65" rx="3"></rect>
          <rect x="30" y="175" width="60" height="55" rx="3"></rect>
        </g>
        <path d="M120 130 L210 275" stroke="#0A0A0A" stroke-width="3" stroke-dasharray="2 5" fill="none" opacity="0.6"></path>
      </svg>
      <div class="homevisit__pin">
        <span class="lbl">{{ settings.atelier_address | default: 'Trakų g. 16' }}</span>
        <span class="dot"></span>
      </div>
    </div>
  </div>
</section>
{% schema %}
{
  "name": "Home — Visit",
  "max_blocks": 4,
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Užsukite" },
    { "type": "text", "id": "title", "label": "Title", "default": "Ateljė — Vilniaus senamiestyje." },
    { "type": "textarea", "id": "text", "label": "Text", "default": "Trakų g. 16, pačiame Vilniaus senamiestyje. Užsukite pasimatuoti arba atsiimti užsakymo." },
    { "type": "text", "id": "cta_label", "label": "Button label", "default": "Adresas ir maršrutas" },
    { "type": "url", "id": "cta_link", "label": "Button link" },
    { "type": "header", "content": "Text size" },
    { "type": "range", "id": "title_scale", "label": "Heading size", "min": 60, "max": 130, "step": 5, "unit": "%", "default": 100 },
    { "type": "header", "content": "Text colour" },
    { "type": "color", "id": "text_color", "label": "Text colour", "info": "Recolours this section's text. Leave empty for the default near-black." }
  ],
  "blocks": [
    {
      "type": "hours",
      "name": "Hours row",
      "settings": [
        { "type": "text", "id": "day", "label": "Day(s)" },
        { "type": "text", "id": "time", "label": "Time" },
        { "type": "checkbox", "id": "closed", "label": "Closed styling", "default": false },
        { "type": "checkbox", "id": "highlight", "label": "Highlight (today)", "default": false }
      ]
    }
  ],
  "presets": [
    {
      "name": "Home — Visit",
      "blocks": [
        { "type": "hours", "settings": { "day": "Pirmadienis", "time": "Uždaryta", "closed": true } },
        { "type": "hours", "settings": { "day": "Antradienis — Penktadienis", "time": "11:00 — 19:00" } },
        { "type": "hours", "settings": { "day": "Šeštadienis", "time": "11:00 — 17:00", "highlight": true } },
        { "type": "hours", "settings": { "day": "Sekmadienis", "time": "Uždaryta", "closed": true } }
      ]
    }
  ]
}
{% endschema %}
